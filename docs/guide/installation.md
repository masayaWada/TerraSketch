# インストール

## pip（推奨）

```bash
pip install terrasketch
```

## 開発版

```bash
git clone https://github.com/wada-masaya/terrasketch.git
cd terrasketch
pip install -e ".[dev]"
```

## 必要要件

- Python 3.10 以上
- 外部依存: `networkx` のみ

## 動作確認

```bash
terrasketch --help
```
